import { FC } from "react";

const AddRecipeForm: FC = () => {
  return (
    <fieldset>
      <form>
        <input type="text" />
      </form>
    </fieldset>
  );
};

export default AddRecipeForm;
